apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
      #---
      #apiVersion: v1
      #kind: PersistentVolume
      #metadata:
      #  name: mysql-pv
      #  labels:
      #    type: local
      #spec:
      #  storageClassName: manual
      #  capacity:
      #    storage: 200Mi
      #  accessModes:
      #    - ReadWriteOnce
      #  hostPath:
      #    path: "/data"
